name: mx_test_app
description: MX Enabled integration test application

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_webview_plugin: 0.3.11
  flutter_inappwebview: 4.0.0+4
  http: 0.12.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
